class Fibonacci {
    
    main(string args, int x, bool y){
        int a = 1?
        a = fib(4)?
        #yolo
        print a?
        print args?
        print x?
        print y?
    };
    
    int fib(int a) {
        if(a == 1) { return 0? }
        else {if(a == 2){ return 1? } 
              else { return fib(a-1) + fib(a-2)? }? }?
    };
}
