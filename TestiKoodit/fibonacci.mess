#Laskee rekursiivisesti n:nnen luvun Fibonaccin sarjasta. n annetaan komentoriviparametrina
class Fibonacci {
    
    main(int n){
        int a = fib(n)?
        print a?
    };
    
    int fib(int a) {
        if(a == 1) { return 0? }
        else {if(a == 2){ return 1? } 
              else { return fib(a-1) + fib(a-2)? }? }?
    };
}
